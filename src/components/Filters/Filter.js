import React, { Component } from 'react';

const filter = (props) => {
  return (
    <div>
      <h2>Filter by:</h2>
      <input type="text" />
      <button>Go!</button>
    </div>
  )
}

export default filter;
